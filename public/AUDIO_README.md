Place five adhan audio files here as `azan1.mp3` … `azan5.mp3`.

These are not bundled with the repo (licensing) — the app degrades gracefully
(play() rejections are caught and surfaced as a toast) if a file is missing.
