import { WidgetCard } from '../../components/grid/WidgetCard';

export function WeatherMinimapWidget() {
  return (
    <WidgetCard centered>
      <div className="text-center text-sm text-slate-400">Open map (optional tile layer)</div>
    </WidgetCard>
  );
}
